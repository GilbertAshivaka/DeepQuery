# ==============================================================
# Deep Query — Document Sandbox image dependencies (Phase a)
# ==============================================================
# This closure is INDEPENDENT of backend/requirements.txt: the sandbox is a
# separate container, so it is free of the backend's umap/torch numpy ceiling.
# python-docx is held at the backend's pin to avoid behavioural drift.
# pptx/pdf libs (python-pptx, reportlab, weasyprint) are deferred to Phase d.

pandas==2.2.3
numpy==2.1.3
matplotlib==3.9.2
openpyxl==3.1.5
xlsxwriter==3.2.0
python-docx==1.1.2
python-pptx==1.0.2
reportlab==4.2.2
